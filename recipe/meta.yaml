{% set name = "seqdat" %}
{% set version = "22.1.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/seqdat-{{ version }}.tar.gz
  sha256: 880e1b4929dfc564c6abf2e2caf44720a3df75c1c529dfab4e08eb02cc6167ac

build:
  number: 0
  noarch: python
  entry_points:
    - seqdat = seqdat.cli:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6,<3.11
    - poetry
  run:
    - click >=8.0.3,<9.0.0
    - click-rich-help >=0.2.0
    - python >=3.6,<3.11
    - rich >=10.12.0,<11.0.0
    - ruamel.yaml >=0.17.17,<0.18.0

test:
  imports:
    - seqdat
  commands:
    - pip check
    - seqdat --help
  requires:
    - pip

about:
  home: https://github.com/daylinmorgan/seqdat
  summary: sequencing data manager
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - daylinmorgan
